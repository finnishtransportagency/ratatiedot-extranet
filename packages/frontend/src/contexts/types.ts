export enum ENotificationType {
  INFO = 'info',
  WARNING = 'warning',
  ERROR = 'error',
  CONFIRMATION = 'confirmation',
}

export enum EContentType {
  CARD = 'card',
  TEXT = 'text',
  FILE = 'file',
}
